import React from 'react';

const EnrolledClasses = () => {
    ///"https://localhost:3000/payments/enrolledCourses"  enrolled(paid class api)
    return (
        <div>
            Enrolled
        </div>
    );
};

export default EnrolledClasses;