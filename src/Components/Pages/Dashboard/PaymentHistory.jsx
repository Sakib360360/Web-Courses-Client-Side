import React from 'react';

const PaymentHistory = () => {
    //`https://localhost:3000/payments/enrolledclass?email=${user?.email}`   payment history api
    return (
        <div>
            payment
        </div>
    );
};

export default PaymentHistory;